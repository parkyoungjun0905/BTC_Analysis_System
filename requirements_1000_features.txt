# 💎 1000+ 특성 비트코인 예측 시스템 의존성
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# 🔷 필수 의존성 (기본 실행)
pandas>=1.5.0
numpy>=1.21.0
sqlite3  # 기본 내장 모듈

# 🔷 기계학습 및 특성 선택 (고급 기능)
scikit-learn>=1.1.0
scipy>=1.9.0
joblib>=1.2.0

# 🔷 고급 수학 분석 (선택사항)
pywt>=1.4.1        # 웨이블릿 변환
pyemd>=0.5.1       # Earth Mover's Distance  
nolds>=0.5.2       # 비선형 시계열 분석

# 🔷 SHAP 기반 특성 중요도 (선택사항)
shap>=0.41.0

# 🔷 기술적 분석 라이브러리 (선택사항)
TA-Lib>=0.4.25     # 고급 기술적 지표
talib-binary>=0.4.19  # TA-Lib 바이너리

# 🔷 시각화 (선택사항)
matplotlib>=3.5.0
seaborn>=0.11.0
plotly>=5.10.0

# 🔷 데이터 수집 (선택사항)  
aiohttp>=3.8.0     # 비동기 HTTP 클라이언트
requests>=2.28.0   # HTTP 라이브러리
yfinance>=0.1.87   # Yahoo Finance API

# 🔷 성능 최적화 (선택사항)
numba>=0.56.0      # JIT 컴파일러
dask>=2022.8.0     # 병렬 처리
multiprocessing    # 기본 내장 모듈

# 🔷 시간 처리
python-dateutil>=2.8.0
pytz>=2022.1

# 🔷 설정 파일
pyyaml>=6.0        # YAML 설정 파일
configparser       # 기본 내장 모듈

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 📋 설치 방법:
# 
# 🔸 기본 설치 (최소 기능):
# pip install pandas numpy scikit-learn scipy
#
# 🔸 완전 설치 (모든 기능):  
# pip install -r requirements_1000_features.txt
#
# 🔸 TA-Lib 설치 (별도 바이너리 필요):
# # Windows
# pip install TA-Lib
# 
# # macOS  
# brew install ta-lib
# pip install TA-Lib
#
# # Ubuntu/Debian
# sudo apt-get install libta-lib-dev
# pip install TA-Lib
#
# 🔸 고급 수학 라이브러리 (선택):
# pip install pywt pyemd nolds numba
#
# 🔸 시각화 라이브러리 (선택):
# pip install matplotlib seaborn plotly
#
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━